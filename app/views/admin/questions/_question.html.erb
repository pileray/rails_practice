<tr>
  <td><%= question.id %></td>
  <td><%= question.title %></td>
  <td><%= link_to "削除", admin_question_path(question), class: "btn btn-danger", data: { turbo_method: :delete, turbo_confirm: "削除しますか？" } %></td>
</tr>